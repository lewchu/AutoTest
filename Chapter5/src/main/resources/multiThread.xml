<?xml version="1.0" encoding="UTF-8" ?>
<suite name="multiThread" parallel="methods" thread-count="2">
    <!--
        methods级别：所有用例都可以在不同的线程下去执行
        thread-count="2"：最大并发线程数
        xml文件配置这种方式不能指定线程池，只有方法上才可以指定线程池
        
        tests级别：不同的test tag下的用例可以在不同的线程中执行，相同的test tag下的用例只能运行在相同的线程中
        
        classes级别： 相同的class tag下的用例在同一个线程中执行，不同的class tag（class需要有name属性进行区分）下的用例可以在不同的线程中执行
    -->
    <test name="demo1">
        <classes>
            <class name="com.lewchu.multiThread.MultiThreadOnXml" />
        </classes>
    </test>

    <!--测试parallel="tests"级别-->
    <!--<test name="demo2">
        <classes>
            <class name="com.lewchu.multiThread.MultiThreadOnXml" />
        </classes>
    </test>-->
</suite>